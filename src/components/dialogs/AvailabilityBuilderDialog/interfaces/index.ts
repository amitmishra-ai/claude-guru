// Component-specific interfaces for AvailabilityBuilderDialog
// Shared domain types (Pattern, PresetCard, etc.) live in src/lib/types.ts

export interface IAvailabilityBuilderDialogProps {
  // No props — dialog open state is driven by Redux (s.ui.openAvailability)
}
