# Changelog

All notable changes to the Guru Dashboard are documented here.

---

## 2026-05-05

### Profile — info tooltips on stats explaining differences from the old dashboard
- **`InfoOutlinedIcon` tooltips** added to three Profile-page stat tiles in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) so gurus comparing numbers between dashboards understand why the new value may differ:
  - **Engagement Count** tile: covers reasons 1 + 2 from the engineer's note. CV review requests are not counted in either dashboard (they now show as 99+ in the Completed tab); we now include all completed online sessions, including those without feedback (the old dashboard counted only sessions with feedback).
  - **Learners Impacted** tile: count may be higher than the old dashboard. The previous calculation was inaccurate; this one is more accurate.
  - **Rating cards** (Teaching / Mentoring / Evaluation / Moderation in the Performance section): rating may be slightly higher than the old dashboard. The new calculation is more accurate.
- New `infoNote?: string` field added to the `EngagementChart` type and to `ratingCards` entries; the existing tile header `Stack` now wraps the label + a small `InfoOutlined` `IconButton` inside an inner `Stack` and falls back to no icon when the field is empty. Tooltip is touch-friendly (`enterTouchDelay: 0`, `leaveTouchDelay: 6000`) and capped at 320px width with smaller font / tighter padding so the message reads naturally on hover or tap.
- Click on the info icon stops event propagation so it doesn't also open the detailed-report drawer.

### Career Mentor — feedback drawer reshaped for 1:1 sessions
- **Demo data updated** in [src/data/demo-sessions.ts](src/data/demo-sessions.ts) so career sessions reflect the 1:1 reality: `career1`, `career2`, `career3` and `ind-1on1` now each have **a single learner rating** instead of dozens, and their entries in `demoFeedbackSummaryBySessionId` use `totalResponses: 1`, `totalEnrolled: 1`, with career-specific parameters: **Understanding my concerns** and **Advice based on my profile** (replacing the teaching parameters like "Concepts covered" that did not apply).
- **`LearnerRatingsDialog`** ([src/components/dialogs/LearnerRatingsDialog.tsx](src/components/dialogs/LearnerRatingsDialog.tsx)) now branches on `isCareer = sessionType === "Career mentoring session"`. For career sessions the multi-learner aggregated cards (rating distribution donut, session benchmark, parameter stacked bars, comments filter tabs) are skipped and replaced with two simpler cards:
  - **Parameter wise rating** — a clean list with the parameter label on the left and a 5-star row coloured by rating (5★ green, 4★ amber, 3★ red).
  - **Learner's comment** — single block with the learner's name and their comment text, no filter tabs.
- The header still reads "Online session feedback" and the session-info card still shows `1/1 No. of feedback` and the single rating, mirroring the old dashboard's framing for this view.

### Career Mentor — richer "Learner Details" section in Activity Details drawer
- **`LearnerContext` type extended** in [src/lib/types.ts](src/lib/types.ts) with the fields the `career_session` API returns: `imageUrl`, `designation`, `companyName`, `experience` (years) and `agenda`. Existing fields (`learnerName`, `resumeUrl`, `linkedInUrl`, `learnerProfileUrl`, `notes`) kept for back-compat.
- **Existing "Learner context" block in [src/components/dialogs/SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx) replaced with a richer "Learner Details" card**. Shown for `Career mentoring session` whenever `learnerContext` exists. Layout:
  - Identity row: 48×48 `Avatar` (uses `imageUrl`, falls back to initials in primary-container colours), name in 600 weight, designation · company in muted secondary, "X years experience" caption below.
  - **Agenda** block in an `overline` label + soft container background pill, `whiteSpace: pre-wrap` so multi-line agendas render cleanly.
  - Action buttons row: "View LinkedIn", "View Resume", "View Profile" — all `variant="soft"` size small with proper `<a target="_blank" rel="noopener">` so the links actually open the URL instead of dispatching toasts.
  - Legacy `notes` field still rendered below if present, in the same muted block style as before.
- **Demo data populated** so the section is visible end-to-end:
  - [src/data/demo-sessions.ts](src/data/demo-sessions.ts): three `Career mentoring session` rows (`career1` Ananya Sharma / Senior Analyst at Infosys; `career2` Karthik Iyer / Software Engineer at TCS; `career3` Meera Pillai / Business Analyst at Accenture) now carry full `learnerContext` with avatar, designation, company, experience and a realistic multi-sentence agenda.
  - [src/pages/Components/demo-component-sessions.ts](src/pages/Components/demo-component-sessions.ts): the three career-session demo rows used by the Components catalogue (`demoCareerConfirmed` Aarav Mehta, `demoCareerCompletedGathering` Priya Sharma, `demoMockConfirmed` Rohan Gupta) extended with the same shape so the Components page also renders the new card.
- Imports: `Avatar` added to [SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx).

### Career Mentor — drawer trimmed to what matters for 1:1 career sessions
- **"Group: 1:1 Session" row hidden** in the Details section ([src/components/dialogs/SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx)) for career mentoring sessions. The session type already implies 1:1, so the row was noise.
- **Attendees section hidden** for career mentoring. The single learner is fully covered by the Learner Details card above, so the "Combined session · 1 members" box no longer appears.
- **Linked course section hidden** for career mentoring. These sessions have no course attached, so the entire block is suppressed.
- **Combined-session accordion on Dashboard cards** ([src/pages/Dashboard/index.tsx](src/pages/Dashboard/index.tsx)) hidden for career mentoring; the mobile View-details suppression also skips the type. Career sessions are 1:1 by definition, no combined-batches affordance needed.
- **Planned activities (subject to change) section hidden** for the Career Mentor role. `rolePlannedEvents` returns `[]` when the selected role is "Career Mentor"; learners self-schedule from the guru's calendar so there is no tentative state.

### Career Mentor — demo session naming aligned with production
- All seven demo career sessions across [src/data/demo-sessions.ts](src/data/demo-sessions.ts) (`career1`, `career2`, `career3`, `ind-1on1`) and [src/pages/Components/demo-component-sessions.ts](src/pages/Components/demo-component-sessions.ts) (`demoCareerConfirmed`, `demoCareerCompletedGathering`, `demoCareerCompletedWithRating`, `demoMockConfirmed`) now use production-style values: title `"Career mentoring session"`, topic `"Career Guidance, Interview Preparation"`, contact `learners_success@greatlearning.in` (Learners Success), and real-world batch names (`Mtech SRM Chennai- DS July 24- Sem 1`, `AIML Online March 25`, `PGPDSBA.O.OCT24.A`, `JAIN-MCA-Sem-1_Jul'24`, `Mtech SRM Chennai- WAI July 24- Sem 1-Section B`, `JAIN-MBA-Sem-1_July22A`).

### Old Dashboard — default landing route + dialog flow + sticky stack + vibrant alert
- **Default landing now `/old-dashboard`** — root path `/` in [src/App.tsx](src/App.tsx) renders `OldDashboardPage`. New dashboard moved to `/new-dashboard`. Sidebar Home (`NavLink` in [src/components/layout/Sidebar.tsx](src/components/layout/Sidebar.tsx)) and MobileNav Home tab ([src/components/layout/MobileNav.tsx](src/components/layout/MobileNav.tsx)) updated accordingly.
- **Dialog no longer auto-opens** ([src/pages/OldDashboard/index.tsx](src/pages/OldDashboard/index.tsx)). It now opens only when the user clicks "Try it now" in the sticky alert. Dialog content shortened to a "What is new" header + four single-line bullets and a one-sentence note that explains how to switch back from the new dashboard's profile menu.
- **Top nav + alert pin together** — both wrapped in a single `.od-sticky-stack` container that is `position: sticky; top: 0`. Earlier the alert was the only sticky element and competing scroll contexts caused it to scroll away. Document-level scroll restored by setting `html` and `body` to `overflow: visible !important` while the page is mounted.
- **Alert restyled to a Bootstrap warning** with a left-side warning-triangle SVG, cream-yellow background `#fcf3cf`, soft border `#f6e58d`, dark amber text `#6b5300`, and a `#337ab7` Bootstrap-primary "Try it now" button. Clearly visible against the page's light blue-gray backdrop.

---

## 2026-05-04

### Old Dashboard — dialog and bar copy cut to scannable length
- Modal lead, bullet list, and footer note all trimmed. Five long bullets with bold leads + explanations replaced by three short one-liners. Footer note reduced to one sentence: "Becomes the default in a few days. You can switch back any time."
- Sticky bar copy reduced to "**The new Guru Dashboard is here.** It becomes the default in a few days."

### Old Dashboard — dialog copy rewritten with concrete, feature-grounded benefits
- **Modal benefit list now references actual new-dashboard features** instead of generic statements (was: "Works well on phone and tablet too", "Upcoming and Completed sessions on one home page" — both already true on the old dashboard). New bullets, sourced from [context.md](context.md) and [Guru Dashboard — Upcoming & Completed Tabs.md](Guru%20Dashboard%20%E2%80%94%20Upcoming%20%26%20Completed%20Tabs.md):
  - **Every activity, not just online sessions.** Residencies, evaluations, moderations, capstone projects and CV reviews now show up under Upcoming and Completed alongside online sessions (the old dashboard only surfaces online sessions cleanly).
  - **A calendar that protects your time.** One leave entry auto-declines conflicting sessions and blocks request slots for that window, so the guru does not have to follow up with their PM (sourced from `context.md` calendar/availability behavior).
  - **Context before you join a 1:1 career session.** Resume, LinkedIn, agenda, company and focus areas show up before clicking Join (sourced from Online Session 1:1 spec).
  - **Payments that add up.** Month-on-month chart, average and best month, plus the transaction ID next to every completed activity (sourced from Profile/Earnings spec + Completed-tab payment transaction ID).
  - **Performance worth opening.** Average rating, monthly trend, course-wise heatmap and rated-session coverage on the Profile page.
- **Lead paragraph rewritten** to: "The Guru Dashboard has been rebuilt around your work as a guru. Sessions, evaluations, capstones, payments, ratings and student details now live on one screen instead of being scattered across pages."
- **Bottom note tightened** to communicate the deadline + safety net + feedback ask in a single paragraph.
- **Promo bar copy** also tightened to "We are switching everyone over in a few days. Take it for a spin and tell us how it feels."

### Old Dashboard — switch UI rebuilt as native Bootstrap 3 alert + modal
- **Promo bar swapped from a custom gradient pill to a real Bootstrap `alert alert-info`** in [src/pages/OldDashboard/index.tsx](src/pages/OldDashboard/index.tsx). Sticky-pinned to viewport top with `position: sticky; top: 0`, full-width edge-to-edge, info palette `#d9edf7` / `#31708f` / `#bce8f1`. CTA is a real `btn btn-primary btn-sm` using the same `#337ab7` / `#2e6da4` blue as the existing "Create/View Polls" button on the page, so the alert visually belongs to this Bootstrap UI. Copy: "**A new Guru Dashboard is ready for you.** We will move everyone to it in a few days. You can start using it today and tell us how it feels."
- **Switch dialog rebuilt as a Bootstrap 3 modal** — backdrop `rgba(0,0,0,0.5)`, white content card with `border-radius: 6px` and `0 5px 15px rgba(0,0,0,0.5)` shadow, classic `modal-header` (15px padding, `#e5e5e5` bottom border, top-right `×` close at 21px / `opacity 0.2 → 0.5` on hover, 18px medium title), `modal-body` with paragraphs and a plain disc `<ul>`, an embedded `alert alert-warning` for the deadline note, and `modal-footer` with `btn-default` (Maybe later) + `btn-primary` (Try it now) right-aligned. No gradients, no oversized type, no animated bouncy entry.
- **Bootstrap primitives now scoped to `.od-body`** (`btn`, `btn-sm`, `btn-primary`, `btn-default`, `alert`, `alert-info`, `alert-warning`) so they only apply on this page and do not leak into the rest of the app.
- **Copy rewritten with a more human tone** — modal opens with "We have rebuilt the Guru Dashboard to make your day on Great Learning a little simpler. Everyone will move to it in a few days, but you can start using it right away.", followed by "Here is what changes for you:" and the four-bullet list. The third bullet now reads "See your ratings, sessions and **payments** in one place" (was "earnings"). The deadline note inside `alert-warning`: "If something does not feel right, you can come back to this dashboard any time from the profile menu. Do tell us what works for you and what does not, so we can fix it before the switch."

### Old Dashboard — humanised promo + dialog copy, em-dashes removed
- **Promo bar copy** in [src/pages/OldDashboard/index.tsx](src/pages/OldDashboard/index.tsx) rewritten to a friendlier sentence: "We have a new Guru Dashboard ready for you. It becomes the default in a few days, so try it out and tell us what you think." CTA pill changed from "Try New Dashboard" to "Try it now".
- **Dialog copy rewritten** to sound less marketing-AI: title "Try the new Guru Dashboard"; subtitle "We have redesigned it to make your day a little easier."; bullets simplified to "Upcoming and Completed sessions on one home page", "Calendar, Courses, Payments and Profile a click away", "See your ratings, sessions and earnings in one place", "Works well on phone and tablet too"; deadline note: "In a few days this will be the default for everyone. Give it a spin and share your feedback. You can come back to the old dashboard any time from the profile menu."; primary CTA renamed to "Try it now".
- **All em-dashes (`—`) removed** from the page; replaced with periods, commas, or rephrased clauses where they previously joined clauses.

### Old Dashboard — top-bar profile dropdown matches legacy UI + adds Switch to New Dashboard
- **Top-right name now reads from Redux `profile.guruName`** in [src/pages/OldDashboard/index.tsx](src/pages/OldDashboard/index.tsx) so the legacy dashboard shows the same real user name as the new dashboard (default "Snehanjan Shome" in dev) instead of the hardcoded "Mr. Onkar Gayakwad" placeholder.
- **Dropdown rebuilt as `<ul>` matching the production legacy menu screenshot** — five items in order: `Your Profile`, `Switch to Learner Dashboard`, `Switch to New Dashboard` (the new one, navigates to `/`), `Refer participants`, `Logout`. Plain text rows, no icons, 1px hairline divider between items, 10px×20px padding, 14px Helvetica Neue, hover background `#f5f5f5` — exactly the Bootstrap dropdown look from the supplied image.

### Old Dashboard — two switch-to-new touch points (launch dialog + sticky promo bar)
- **Launch dialog on every visit to [src/pages/OldDashboard/index.tsx](src/pages/OldDashboard/index.tsx)** — On mount, a centred modal opens encouraging the guru to try the new dashboard. Hero strip uses the same purple/blue gradient as the promo bar with a "NEW" pill, title "Meet the new Guru Dashboard", and a one-line subtitle. Body lists four concrete benefits (cleaner home, quicker access to Calendar/Courses/Payments/Profile, better performance & earnings views, mobile-friendly) with rounded check bullets. A soft yellow note communicates the deadline messaging requested by the user: "The new dashboard will become the default for everyone in a few days. Try it now and share feedback — you can always switch back from the profile menu." Footer has two CTAs — secondary `Maybe later` (closes dialog) and primary gradient `Try New Dashboard` (navigates to `/`). The dialog is dismissable via the close `×`, backdrop click, or Maybe later, and locks body scroll while open via a temporary `od-no-scroll` class. Pop-in scale animation + fade backdrop.
- **Sticky promo alert bar** — Sits between the white top app bar and the Upcoming/Completed tabs. Uses `position: sticky; top: 0; z-index: 50` so it stays pinned to the viewport top once the user scrolls past it (modeled after Figma's "payment pending" alert bar referenced by the user). 90deg navy → blue → purple gradient with a sparkle icon, the message "A redesigned **Guru Dashboard** is here — faster, cleaner, and built around your day. It becomes the default in a few days.", and a white pill `Try New Dashboard →` CTA aligned right. Bar persists across page scroll so the switch action is always one click away even if the dialog has been dismissed. Mobile-aware: at ≤600px the layout wraps and font/padding shrink so it doesn't overwhelm small screens.

### Sidebar + Account — "Switch to Old Dashboard" entry + new `/old-dashboard` route
- **Profile menu in [src/components/layout/Sidebar.tsx](src/components/layout/Sidebar.tsx) gained a "Switch to Old Dashboard" item** placed directly under "Switch to Learner Dashboard". Click navigates to `/old-dashboard`. Uses the existing `SwapHorizOutlinedIcon` for visual consistency with the sibling switcher.
- **Mobile [src/pages/Account/index.tsx](src/pages/Account/index.tsx) `accountSection` got the same entry** under the "Switch to Learner" row so desktop sidebar and mobile account page stay in parity. Sublabel: "Open old guru dashboard".
- **New page [src/pages/OldDashboard/index.tsx](src/pages/OldDashboard/index.tsx)** — 1:1 replica of the legacy guru dashboard built directly from Figma node `352:3` of the GL Design System Testing file. Every token (colors, sizes, spacing, fonts) is taken from `get_design_context`: top bar `#f8f8f8` with `#e7e7e7` border and 4px radius, centred "Great Learning Gurus" title in Helvetica Neue Medium 24px / `#333`, right-aligned `#196ae5` round badge + `#337ab7` link list with dashed-triangle caret; tabs use `#00bfff` cyan underline (active tab gets white bg + `#ddd` border), 18px Helvetica Neue Bold, inactive `#696969`; "New" pill is solid red, GL Profile gets a small graph icon; availability banner uses the exact Figma gradient `linear-gradient(260.99deg, rgba(65,88,232,0.2) 2%, rgba(248,107,255,0.2) 94%)`, 6px radius, 19px padding, 24px medium title, 14px regular subtitle, `#017bfe` `View/Add` pill button; section headers are 22px white text on `#12cd66` (green) and `#ffc82c` (yellow) with inset shadow + 4px radius matching Bootstrap panel-heading; event cards reuse exact figma widths (date col 252.5px with absolutely-positioned 53×60 headset icon at 15/8, big 32px Helvetica Bold day, 26px Helvetica `#696969` month; body box 420.83px wide, outer 13px padding + inner `rgba(224,225,226,0.5)` overlay with 10px padding, title `#76abe4` 18px, body lines 18px `#000` with `0.36px` tracking, glyphicons-style envelope ✉ in `#7c7f82`; action col 336.66px with centred 18px `#696969` time, blue `#00bfff` "Join session" link with cam-video icon, and `#337ab7` / `#2e6da4` "Create/View Polls" button on the first card only). Empty state row "No planned events at this moment!" rendered italic 18px `#333` per Figma. Five Figma assets (GL logo SVG, calendar SVG, graph-up SVG, cam SVG, headset PNG) downloaded from the MCP server into [public/old-dashboard/](public/old-dashboard/) so the page works after the 7-day Figma asset URL window expires. The page injects no external CSS — all styling is inline `<style>` scoped via `.od-*` classes and a body class added on mount and removed on unmount, so the design system used by the rest of the app is never affected. Route registered outside the [AppLayout](src/components/layout/AppLayout.tsx) wrapper in [src/App.tsx](src/App.tsx) so there is no sidebar, no mobile nav, no top app bar — completely standalone old UI as requested. The user dropdown exposes a "Switch to New Dashboard" item that navigates back to `/`.

---

## 2026-04-30

### Payments - metric tile consistency + chronological earnings chart
- **Total earnings tile now matches sibling metrics: label-on-top, value-below** — In [src/pages/Payments/index.tsx](src/pages/Payments/index.tsx) the leading `Total earnings` block had its `h5` value rendered above the `caption` label, while the sibling tiles in the same flex row (`Avg/month`, `Best month`, `MoM trend`) all use label-on-top, value-below. Inverted the Total earnings stack so all four tiles share the same vertical structure (`caption` label → bold value). Total earnings keeps its larger `h5 / weight 700` value to remain the visual anchor of the row, but the layout itself is no longer inconsistent. Switched the FlexBox `alignItems` from `center` to `flex-start` so taller tiles don't push shorter ones into mis-aligned baselines.
- **Earnings chart Y-axis pinned so tick labels (`80k`, `60k`…) stay visible at any scroll position** — Single-chart layout meant the Y-axis lived inside the wide scrolling SVG and slid off-screen when the user reached the right edge. Split the chart into two adjacent recharts: a fixed-width 50px sticky strip on the left rendering only the Y-axis, and the existing horizontal scroller on the right rendering bars + X-axis with its own Y-axis hidden (`<YAxis hide />`). Both charts share an explicit `chartYDomain` (`[0, ceil(max * 1.1 / 5000) * 5000]`) so bar heights and tick positions stay aligned regardless of scroll. Both charts use matching top/bottom margins + `XAxis height={22}` (left axis with `tick={false}`) so plot rectangles line up.
- **All-time earnings bar chart restored to ascending chronological order with auto-scroll to latest** — Chart was reversing the source data so the latest month sat on the left and scrolling right uncovered older history — readable but inverts the natural left-to-right time axis convention. Dropped the `[...src].reverse()` so `chartData` is now ascending (Jan 2021 → Mar 2026, oldest left → latest right). Updated `momTrend` to read from the tail of the array (`length-1` minus `length-2`) instead of indices 0/1. To preserve the convenience of the previous design (latest months in view without manual scrolling) added a `chartScrollRef` on the horizontal scroll container and a `useLayoutEffect` that sets `el.scrollLeft = el.scrollWidth` after data is ready, so the viewport lands on the right edge on mount; scrolling left reveals older history. Re-runs when `chartData.length` changes (e.g. empty-state toggle). Imported `useLayoutEffect` + `useRef` from React.

---

## 2026-04-29

### Activity Details - Combined session attendees simplified
- **Unified "Attendees" header + flat batch rows (no accordion)** — In [src/components/dialogs/SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx) the Group-only and Combined-session sections each used their own ad-hoc heading (`{session.group} · 6 learners`, `Combined session · 64 members`) so they read as orphaned blocks rather than an answer to "who's joining this activity". Both sections now share a single `Attendees` SectionHeading. For combined sessions a small caption underneath still gives the roll-up (`Combined session · 64 members`). Removed the per-row Accordion entirely — no more `Members (N)` learner-name/email expansions, no more "Group details" sub-block, no more `Individual students` row type. Each entry collapses to a single static row: title = batch name (e.g. `AIML Online March 26 A`), sub-line = `42 learners`, with the existing Whole-batch / Group / Individual chip on the right. Individual entries now render as `{batch} · 1 learner` (the student name is no longer displayed in this drawer). For the standalone Group session the duplicated label (header `Group 02 · 6 learners` + accordion summary `Group 02 · AIML Online March 26 A · 6 learners`) is gone — single card now shows `{group} · {batch}` once with `N learners` underneath. Dropped `expandedGroup` / `expandedCombinedBatch` `useState`s and the now-unused `Accordion` / `AccordionSummary` / `AccordionDetails` / `Avatar` / `ExpandMoreIcon` / `MenuBookOutlinedIcon` imports.

### Activity Details - Single Attendees header + Individual rows split
- **One `ATTENDEES` SectionHeading covering both Group and Combined-session blocks** — Previous pass rendered two stacked `ATTENDEES` headers (one with the people icon for Group, another with the merge icon for Combined) which read as separate sections rather than two halves of the same answer. Now [src/components/dialogs/SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx) renders a single `Attendees` SectionHeading whenever either source has data, with two sub-headers underneath: `Group` (uppercase caption) when `groupMembers` exists, and `Combined session · {N} members` when `combinedBatches` exists. Both can render together in the same Attendees section if a session has both. Dropped the `CallMergeOutlinedIcon` import.
- **Attendees row chips use default MUI sizing** — `Group` / `Whole batch` / `Individual` chips were forced down to `height: 18, fontSize: "0.55rem", borderRadius: "4px"` which made the label illegible. Removed the size override; chips now render at MUI `size="small"` defaults (24px height) with `fontSize: 0.75rem` (12px Caption token) and `fontWeight: 600`.
### Profile - per-category Performance blocks made collapsible
- **`{Category} Performance` cards converted to accordions, default-expanded** — Each per-role-category block on the Profile page (Mentoring Performance, Evaluation & Moderation Performance, Teaching Performance) wraps a 4-up KPI grid + a 12-month rating matrix table. For multi-role users, the second/third block was being pushed below the fold and effectively never seen. In [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) replaced the outer `Card` + `CardContent` with `Accordion` + `AccordionSummary` (title) + `AccordionDetails` (KPIs + table). All accordions render expanded by default, so a single-role user sees no behavior change and a multi-role user can collapse the first block to surface the next one without scrolling. Kept the outlined card look (`border 1 / divider / radius 12`), preserved heading typography (h6, 0.95–1.05 rem, weight 700), and matched the AccordionSummary padding to the original card's px so vertical rhythm doesn't shift. Picked accordion over a tab bar at the user's request — tabs would have hidden alternate categories until clicked, whereas accordion keeps everything visible by default and only collapses on intent.

- **Performance accordion collapsed-state strip: dropped synthetic avg rating, restructured for scannability** — Round 4. (1) Removed the leading ⭐ avg block. The "4.6 avg / 4.7 avg" was computed locally as the mean of matrix cell scores in the body — it isn't part of the KPI cards backing the accordion, so it read as a phantom number with no source the user could reconcile. Strip now shows only the actual KPI card data the body renders. (2) Restructured each KPI from a single label-then-value line into a stacked mini-block: row 1 = value (15px / 700 primary) with optional ↑/↓ delta beside, row 2 = compact label (11px / secondary, capitalised). Items separated by taller 1×24 dividers, spaced `2 / rowGap 1`. Value-up / label-down lets the eye scan the row of bold numbers first, then refer down for context — much faster than reading "Avg sessions / Month 10 ↗ +1 | Avg session quality 97.5% ↗ +0.8%" as one long sentence. (3) Label simplification: `compactLabel()` lowercases, strips leading `avg`, keeps `/ Month` spelled in full (`AVG SESSIONS / MONTH` → `Sessions / Month`). Capitalisation handled by the `textTransform: capitalize` style on the rendered Typography. Removed the unused `avgScore` calc + helper. — Round 3 polish. (1) Collapsed-state strip now renders **every** KPI card from `categoryStats` (not just the first), so the same data the body would show — `Avg sessions / Month 10`, `Avg session quality 97.5%`, `On-time confirms 94%`, etc. — is visible inline when collapsed. Each entry: humanised label (lowercase + sentence-case + `/ Month` recapitalised) at 11px / 400 secondary color, then value at 13px / 600 primary, then optional ↑/↓ delta. Items are separated by a thin 1×14 divider via `Stack divider`, and the strip wraps with `flexWrap: "wrap" + rowGap: 0.5` so a 4-card category doesn't overflow on smaller widths. Avg ⭐ score still leads. (2) Chevron now has its own hover state — targeted `.MuiAccordionSummary-expandIconWrapper` with `width: 32, height: 32, borderRadius: 50%`, idle color `text.secondary`, hover `bgcolor: action.hover` + `color: text.primary` (150ms transition) — so the affordance reads as a clickable circular icon button rather than just a static glyph that rotates. Whole-row hover from round 2 is still there. — Round 2 polish. (1) State persistence: expand/collapse choice now lives in `localStorage` under `profile.performance.expanded` keyed by category, restored on mount via lazy `useState` initializer and synced via `useEffect`. So if a guru collapses Mentoring Performance once, it stays collapsed across reloads / navigations. Default for any unseen category is expanded so the first-load behavior is unchanged. Helpers `isPerfExpanded(cat)` / `togglePerfExpanded(cat)` keep the controlled-Accordion API tidy. (2) Collapsed-state preview: when collapsed, the AccordionSummary now shows an at-a-glance stat strip on the right (sm+ only — hidden on xs to keep the title legible). Strip contents: gold ⭐ + average matrix score (mean of all non-null scores in `displayRows`) labelled `avg`, a thin divider, then the first KPI card's `value` + a humanised label (`AVG SESSIONS / MONTH` → `sessions/mo`), and a small `TrendingUp/Down` + delta % from `volumeCard.delta` when available, color-coded `success.main` / `error.main`. When expanded the strip is hidden — KPI cards inside the body already carry the same data, so showing it twice would be noise. (3) Subtle `borderColor` hover transition on the whole accordion to telegraph clickability. Net effect: the section delivers value even when collapsed (you see avg rating + trend without expanding) and respects the user's revealed preference across sessions.

- **Whole batch / Group / Individual chips dialed down** — Bold semibold (600) labels at 22px height with default text color drew the eye to the right edge of every Attendees / Combined-session row, fighting the actual content (batch name + count). Lightened across [src/components/dialogs/SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx) (Attendees rows), [src/pages/Dashboard/index.tsx](src/pages/Dashboard/index.tsx) (upcoming activity card combined accordion) and [src/pages/Components/index.tsx](src/pages/Components/index.tsx) (3 showcase mocks): `fontWeight 600 → 500`, `height 22 → 20`, `color → text.secondary`, `borderColor → divider`. Still 12px / Olympus Caption-token sized — just visually quieter, recedes to the edge instead of dominating it.
- **Combined-session inline accordion on activity cards matches drawer Attendees rows** — Upcoming activity card had a `Combined session` accordion with rows like `[•] AIML Online March 26 A — All Groups` and a small `120 learners` pill underneath, which read differently than the drawer's `{batch} · {N} learners + chip` rows. Updated the row template in [src/pages/Dashboard/index.tsx](src/pages/Dashboard/index.tsx) and the equivalent showcase blocks in [src/pages/Components/index.tsx](src/pages/Components/index.tsx) (Residency, Mentoring Confirmed, Mentoring Scheduled) to a single-line row: `{batch} · {N} learners` (12px / 600 batch name + 12px / 400 secondary count) on the left, outlined Whole batch / Group / Individual chip (12px / 600, 22px height) on the right, `justify-between`. Dropped the leading dot, the `— {group}` redundancy, and the small bottom-row count pill. For Individual rows the dashboard mapper now `flatMap`s over `cb.members` so each learner becomes its own row of `{batch} · 1 learner`, mirroring the drawer split. Chip labels harmonized: `Batch → Whole batch`. Note: per the components-source-of-truth feedback memory, both production card and Components mocks updated together.
- **Section-heading-icon consistency rolled out to peer right-side drawers** — Same rule (every overline section heading carries a leading icon) applied beyond `SessionDetailsModal.tsx`. In [src/components/dialogs/CompletedSessionDetailDialog.tsx](src/components/dialogs/CompletedSessionDetailDialog.tsx) — used by Courses + Components + Dashboard for completed sessions — added `InfoOutlinedIcon` to the `Details` heading and `BoltOutlinedIcon` to the `Actions` heading. In the inline planned-activities drawer at [src/pages/Dashboard/index.tsx](src/pages/Dashboard/index.tsx) (right-side drawer for "Planned activities" cards) added `InfoOutlinedIcon` to its `Details` heading. The remaining right drawers (`SessionMaterialsDrawer`, `LearnerRatingsDialog`, `TicketDetailDrawer`, `DevPanel`) don't follow the SectionHeading-overline pattern and were left untouched. SessionDetailsModal already powers the Dashboard / Payments / Courses / Calendar / Components activity-details opens via redux state, so its icon updates were already live everywhere it's invoked.
- **All section headings in Activity Details drawer now have leading icons** — Headings were inconsistent: Attendees / Recording / Feedback / Remuneration / Polls had icons, while Details / Linked course / Session materials / Learner context did not. Added contextual icons so every section heading carries one — Details: `InfoOutlinedIcon`, Linked course: `MenuBookOutlinedIcon`, Session materials: `FolderOutlinedIcon`, Learner context: `PersonOutlinedIcon`. All at `fontSize: 14` to match the existing icon sizing on other headings.
- **Attendees rows compacted to 12px text** — Subtitle 2 (14px) made each row tall and the whole Attendees section dominated the drawer. Dropped both title and subtitle to 12px (Caption token), keeping weight contrast — title 12px / 600, sub-line 12px / 400. Padding tightened (`px 1.75 → 1.5`, `py 1.25 → 0.875`), radius `12 → 10`, chip height `24 → 22` to match the smaller row. Stays on the Olympus scale (10 / 12 / 14…) — no off-scale font sizes.
- **Attendees rows redesigned: filled surface, no leading icon** — Plain outlined boxes felt hollow next to the filled `Details` SectionCard above them. Extracted a new `AttendeeRow` helper in [src/components/dialogs/SessionDetailsModal.tsx](src/components/dialogs/SessionDetailsModal.tsx) so every row shares the same layout. Row surface matches `SectionCard`: `bgcolor: hsl(var(--md-surface))`, divider border, 12px radius, `px 1.75 / py 1.25`. Title bumped to subtitle2 + `fontWeight: 600` with `noWrap` for long batch names; caption sub-line for the count. Chip stays right-aligned. (Tried adding a 36×36 leading icon avatar — dropped it because the icon would be `Groups` for almost every row and `Person` only on rare Individual rows, so it added clutter without conveying differentiation; the right-aligned chip already does that job.) Group sub-section gets a touch more breathing room before Combined (`mb: 1.75 → 2`). Replaced 3 inline row implementations with a single `<AttendeeRow title subtitle chipLabel />` call site.
- **Attendees typography aligned to Olympus design system** — Reference: https://gl-mui-design-system.vercel.app/foundations. Olympus type scale is 10/12/14/16/18/20/24/28/32 px with 400/500/600 weights only. The Attendees rows were rendering off-scale: row title was `caption` (12px) bumped to weight 600, and the secondary line was a hard-coded `0.6rem` (~9.6px). Aligned to: sub-section labels (`Group`, `Combined session · {N} members`) → `variant="overline"` at 10px / 600 (Overline token); row title (batch name / `{group} · {batch}`) → `variant="subtitle2"` (14px / 500, Subtitle 2 token) without ad-hoc weight overrides; row secondary (`{N} learner(s)`) → default `variant="caption"` (12px / 400, Caption token). Drops every off-scale `fontSize: "0.6rem"` in this section.
- **Individual batches always render as `1 learner` rows** — Combined-session demo data lets a single `Individual` entry carry `learnerCount: 2` with a `members[]` of two students. The previous flat-row pass surfaced this as `AIML Online Jan 26 B · 2 learners · Individual` — but per spec each Individual = exactly one learner. The combined-batches mapper now `flatMap`s Individual entries: when `members[]` has multiple, each member becomes its own row of `{batch} · 1 learner · Individual`; otherwise a single `1 learner` row. Combined-member roll-up in the sub-header counts `members.length` (not `learnerCount`) for Individual rows so the total reflects the row split.

---

## 2026-04-21

### Payments - all-time earnings chart, latest-first, scrollable
- **Top earnings chart now shows every month from Jan 2021 → Mar 2026 (63 bars) instead of a rolling 12-month window** — and it renders **descending** (latest month on the left, scrolling right reveals older history). Data in [src/data/demo-sessions.ts](src/data/demo-sessions.ts): replaced the hand-written 6-month `demoMonthlyEarnings` with a deterministic IIFE generator that produces 63 stable monthly entries with a realistic ramp (base 12k + ~11.4k/year growth), a 12-month seasonal multiplier, and a deterministic jitter so values don't look flat. Payments page in [src/pages/Payments/index.tsx](src/pages/Payments/index.tsx): dropped the now-unused `build12MonthChart` helper and `MONTH_SHORT` constant; new `chartData` memo does `[...demoMonthlyEarnings].reverse()` to surface latest-first; `momTrend` recalculated as `arr[0] - arr[1]` since the array is now descending. The chart wrapper wraps the `ResponsiveContainer` in a horizontal scroller (`overflowX: auto` + thin 6px webkit scrollbar) with `minWidth = bars × 36px` so each bar stays readable; XAxis `interval={0}` forces every month label to render. Chart height bumped 160 → 180 to give the scrollbar room, skeleton height matched.

---

## 2026-04-20

### Profile - Share Your Impact redesigned as editorial block
- **Replace nested "thumbnail + stats strip + two CTAs" layout with a single editorial impact block** — Previous design was a bordered card containing a postage-stamp preview of the downloadable share card on the left, a 5-cell stats strip on the right, plus a caption and two buttons. It read as a utility wrapper around a tiny image and duplicated data that Engagement Stats already shows. New design in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx): app-theme-aware surface (`bgcolor: background.paper` + subtle `radial-gradient` accent in `alpha(primary.main, …)` so it adapts to light AND dark — first pass used `shareTheme.bg` which is tuned for the downloadable card and produced washed-out navy in light mode and an out-of-place jade-green in dark), decorative accent circle also keyed to `primary.main` with alpha. Top bar with overline + tagline on the left and the existing period picker (All Time / Month) on the right; inactive outlined buttons no longer force `bgcolor: background.paper` so the outline + text stay readable against the tinted surface. Body is editorial: hero stat = `{learners} learners` in 3.5rem/800 weight with `-0.03em` tracking, narrative subtitle ("impacted since you joined Great Learning" / "reached in {prettyMonth}" — using `shareMonthOptions` label so we get "March 2026" not `activeShareData.monthLabel` which returns shouty "MAR 2026"), `Top 10% of Faculty` chip now tinted `success.main` alpha for clear recognition on both themes. Below: **4** supporting tiles (Sessions, Hours, Avg rating, **Sessions rated 4+**) — this last tile was dropped in the first editorial pass and re-added after review since the 92% 4+ rating is a unique data point not covered elsewhere on the page. Tiles use `alpha(text.primary, …)` fill so they visibly separate from the block surface in both themes. Grid: `repeat(2, 1fr)` on xs, `repeat(4, 1fr)` on sm+. Footer: single `Share my impact` contained CTA + text `Preview share card` link — both open the existing `shareOpen` dialog where the full downloadable share-card preview and social buttons live. Kept `shareCardContent` const + Dialog + mobile month-picker Drawer intact; only the on-page surfacing changed. Dropped the three exploratory options (Hero Poster / Polaroid / Platform Gallery) and the unused `thumbScaleXs/Sm` helpers. Added `alpha` to the `@mui/material/styles` import.

### Profile - Share Your Impact thumbnail preview restored
- **Add the scaled share-card thumbnail back into the editorial block** — Kept the editorial UI (soft app-theme surface, period picker, hero stat, chip, 4 stat tiles, Share CTA) but reintroduced a clickable 180px-wide pixel-perfect thumbnail of the downloadable share card on the left column on desktop. The block body became a 2-column grid (`${thumbW}px 1fr` on sm+, stacks on xs): left = thumbnail with hover Preview overlay (same treatment as the original card), right = editorial content (hero number + subtitle + chip, 4 tiles, Share CTA). Click or hover on thumbnail opens the `shareOpen` dialog with the full-size preview + download + social buttons. Re-added `VisibilityOutlinedIcon` import and a `thumbScale`/`thumbW`/`thumbH` helper trio.

### Sidebar & MobileAppBar - profile photo didn't reflect after upload
- **Wire uploaded profile photo into the bottom-left sidebar and mobile app bar avatars** — Uploading a photo in Preferences dispatched `setGuruPhoto(...)` (writing to `s.profile.guruPhoto` and localStorage), but the chrome avatars were rendering initials-only and never read `guruPhoto`. Added `guruPhoto` selector + `src={guruPhoto ?? undefined}` prop on the two [src/components/layout/Sidebar.tsx](src/components/layout/Sidebar.tsx) Avatars (collapsed + expanded) and the [src/components/layout/MobileAppBar.tsx](src/components/layout/MobileAppBar.tsx) Avatar. Initials remain as the fallback when no photo is set.

### Profile - Share Your Impact vertical compaction
- **Cut Share Your Impact block height by ~45%** — Editorial block was taking too much vertical real estate. Tightened in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx): (1) dropped "Your story, ready to post." tagline and the decorative accent `<Box>` (radial gradient in `backgroundImage` already carries the color pull), (2) outer padding `py 3/4 → 2/2.25`, `px 2.5/4 → 2/2.5`, radius `20 → 16`, (3) top bar collapsed to single row always (was a stacked column on xs), overline + period picker inline, (4) hero reformatted: big number and "learners · {subtitle}" now render inline as one line instead of stacked number → subtitle → chip, number `2.25/3.5rem → 1.75/2.25rem`, inline descriptor `"learners · since you joined Great Learning"` / `"learners · in March 2026"`, `Top 10%` chip height `26 → 22`, (5) Share CTA moved from its own row below tiles into the same row as the hero on desktop (right-aligned, `size small`), stretches full-width on mobile below the hero, (6) dropped the secondary `Preview share card` text link (clicking Share my impact opens the same dialog), (7) 4 stat tiles: padding `1.25/1.5 → 0.75/1`, value font `1.25/1.5rem → 1/1.2rem`, radius `12 → 10`. Removed the now-unused `VisibilityOutlinedIcon` import.

### Profile - Engagement Stats chart parity with main
- **Restore continuous monthly area chart with hover tooltip for Experienced stage** — After the V1 bundle, the Engagement Stats cards for the Experienced stage were rendering yearly-aggregated dot checkpoints (4 points: '23/'24/'25/'26) with dots only revealing a value on hover. This dropped the smooth 2023→2026 ramp that main/prod still shows. Reverted the chart body in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) to main's form: monthly data (39 points Feb 23 → Apr 26), continuous `Area` with `dot={false}`, `interval={11}` on XAxis so only year-anchored ticks show, tooltip on hover along the line via `activeDot`. Mid-user path kept intact (6 months, `interval={0}` so all month ticks show). Removed the now-unused `aggregateYearly` helper.

---

## 2026-04-15

### Profile - Structured per-category Performance blocks (Stats + Avg. month-on-month trends)
- **Replace flat compact stats with subsection-structured category blocks** — Previous merge put single-line stat summaries above the matrix, which was too flat and lost the visual card treatment. Restructured each per-category card into a clearer two-subsection layout with proper subsection headers. **(1)** Extracted `renderCard` + its supporting dictionaries (`zeroMessages`, `earlyValues`, `earlyDescriptions`) out of the old IIFE up to component scope in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) so both the top Ratings row *and* the per-category Stats subsection share the same full card template (label overline, hero number, delta, peer benchmark, sparkline with tooltips). **(2)** Collapsed the IIFE - top Ratings row now renders directly at component render level. **(3)** Per-category card structure: bold `{Category} Performance` heading (h6, 1.05rem), then **"Stats"** overline + grid of full KPI cards (2 cols xs, `min(n, 3)` cols sm+), then **"Avg. month-on-month trends"** overline + course × month matrix table. Visual hierarchy: card title → subsection overlines → content. **(4)** Card padding increased to `p: { xs: 2, sm: 2.5 }` for breathing room. Stats grid uses `Math.min(categoryStats.length, 3)` columns so 1-card groups (V1 mode teaching/mentoring) don't stretch one card full-width, and Eval & Mod's 2 cards sit side-by-side.

### Profile - Merge Operational zone into per-category Performance blocks
- **Collapse standalone "Operational" zone into each category's Monthly Rating Trends card** — Previously a user's data for "Mentoring" was fragmented across three page zones (Ratings row headline, Operational volume cards, Monthly Rating Trends table) forcing them to scroll and reassemble the picture. Merged the operational volume cards INTO the Monthly Rating Trends card of the same category so everything about one category sits in one block (Gestalt proximity). Kept the top Ratings row intact - it's the cross-category at-a-glance compare and has a unique job. Implementation in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx): **(1)** New `operationalCardsByCategory: Map<category, cards[]>` derived from `visibleOperationalGroups`; Evaluation + Moderation groups fold to `"Evaluation & Moderation"` so eval + mod volume cards appear together in the Eval & Mod card. **(2)** Deleted the standalone `visibleOperationalGroups.map(...)` render block from the Performance IIFE. **(3)** Each Monthly Rating Trends card now renders a compact clickable stat summary row above the matrix: label (0.62rem uppercase overline) + value (1.35rem bold) + delta (TrendingUp/Down + colored percent). Clicking a stat opens the same `reportModal` drawer the full cards opened - drill-down preserved. **(4)** Renamed card title from `{Category} Monthly Rating Trends` to `{Category} Performance` so the title covers both the stats and the matrix. **(5)** Stat row hidden for new/early users (`!isNewOrEarly`) and automatically empty if V1 mode filtered all operational cards out of a category.

### V1 Mode - feature gate for Performance zone
- **Wire V1 Mode to hide ON-TIME * and AVG SESSION QUALITY cards** — First consumer of the Dev Panel `isV1Mode` toggle. When V1 mode is ON: **(1)** ON-TIME CONFIRMS hero card is dropped from the top row (Ratings + hero grid becomes ratings-only); **(2)** ON-TIME EVALUATIONS and ON-TIME MODERATIONS operational cards are filtered out of Evaluation / Moderation groups; **(3)** AVG SESSION QUALITY operational card is filtered out of Teaching / Mentoring groups. Leaves the remaining v1 scope intact: per-category rating cards (Ratings zone) + EVALUATIONS / MODERATIONS / SESSIONS per month (Operational zone). Implementation in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx): added `isV1Mode` selector + `v1HiddenLabels = Set(["ON-TIME EVALUATIONS", "ON-TIME MODERATIONS", "AVG SESSION QUALITY"])`; new `visibleOperationalGroups` memo filters each group's cards and drops empty groups entirely; `operationalCards` (drawer-lookup flat list) now derives from `visibleOperationalGroups` so clicking a hidden card is impossible; `statCards` union drops `onTimeHeroCard` when v1; render site uses `visibleOperationalGroups.map(...)` and `topRowCards = isV1Mode ? [...ratingCards] : [...ratingCards, onTimeHeroCard]`. Group 2-col grid handles single-card groups gracefully (second column empty).

### Profile - Engagement Stats
- **Remove "Rating Trend" card from Engagement Stats** — Rating is already covered by the per-category rating cards in the Ratings zone and the per-category Monthly Rating Trends tables further down, so the Engagement Stats "Rating Trend" card was redundant. Dropped the card + its `isRating` / `labels` branches in the render loop (tooltip text, YAxis domain, x-axis labels simplified). Grid shrinks from `repeat(4, 1fr)` to `repeat(3, 1fr)` on `md+` so the three remaining cards (Engagement Count, Engagement Hours, Learners Impacted) fill cleanly. `ratingChartData` and `avgRating` memos kept - still consumed by the rating trend section earlier on the page.

### Global cleanup
- **Remove em-dashes (U+2014) across src/** — Bulk `perl -CSD -i -pe 's/\x{2014}/-/g'` across every `.ts/.tsx/.css` under [src/](src/). 32 files touched. Em-dashes in both visible UI strings and source comments/JSDoc replaced with plain ASCII hyphens. No functional change, typecheck clean.
- **Drop Monthly Rating Trends subtext and title em-dash** — Each per-category card title was `"{Category} - Monthly Rating Trends"` with a caption underneath describing "Avg. month-on-month course ratings across your {category} work". Removed both: title is now just `"{Category} Monthly Rating Trends"` (no dash separator), caption deleted. Kept `mb: 1` on the title so the table still has breathing room below.

### Profile - Monthly Rating Trends split by role category
- **Split "Monthly Rating Trends" from one overall table into one table per active role category** — The single table previously mixed all courses across all active roles (Teacher + Course Mentor + Evaluator etc.) into one overall view, which hid category-level performance signal. Now renders one card per active category (Teaching, Mentoring, Evaluation & Moderation) — so a user with roles across all three sees three separate tables like "Teaching — Monthly Rating Trends", "Mentoring — Monthly Rating Trends", "Evaluation & Moderation — Monthly Rating Trends". Added `matricesByCategory` memo in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx): for each active category it filters `selectedRoles` to the roles in that category, flat-maps each role's `demoRoleMatrix` course rows, and dedupes by course name so the same course doesn't appear twice if two roles in the same category happen to share it. Falls back to `defaultMatrix` when a category has no rows. Each card has a subtitle ("Avg. month-on-month course ratings across your {category} work.") for context. The single `demoMatrix` is still computed for the drawer's per-course lookup at line 2091, unchanged. Empty-state handling (new/early user) applied per card rather than globally.

### Settings — Grid + atomic field edit fixes
- **Fix: remove Settings-only `maxWidth: 720` that broke the app grid** — `AppLayout` already wraps every routed page in `Box sx={{ mx: "auto", maxWidth: "72rem" }}`. The redesign introduced a second `maxWidth: 720` wrapper inside [src/pages/Preferences/index.tsx](src/pages/Preferences/index.tsx) which made the Settings page noticeably narrower than every other page on wide screens — breaking grid consistency with Dashboard, Courses, Profile etc. Removed the inner wrapper so Settings inherits the same 72rem container as the rest of the app.
- **Name/Email: read-only rows with explicit Save via dialog (no auto-save on keystroke)** — Previous design used an always-open `TextField` that dispatched `setGuruName` / `setGuruEmail` on every keystroke — no affordance for intent ("am I editing or just looking?"), no validation checkpoint, every keystroke wrote to Redux + localStorage. Replaced with Airbnb/Google-Account pattern: rows show **read-only** current value as row description + **"Edit ›"** text button. Clicking opens a focused modal (`EditFieldDialog`) with: autoFocus TextField, inline error helper, Enter-to-save, **Save** (contained, disabled until dirty + non-empty) and **Cancel** (discards draft). Save validates (name ≥ 2 chars, email contains `@` + `.`), dispatches once, shows a toast. Added local `editField: "name" | "email" | null` state. Timezone row stays structurally similar — value as description, "Change ›" button opens the existing global `TimezoneDialog`.

### Settings — Full page redesign (Apple System Settings pattern)
- **Redesign Settings page: unified row anatomy, tighter rhythm, capped measure** — Previous page had three issues: (1) section gaps too large (`mt: 3, mb: 1.5` around h6-style titles wasted ~60px per section, three sections ≈ 180px of empty space); (2) each section used a different internal pattern (Profile = stacked caption+field blocks with `mb: 2.5`, Appearance = centered flex, Communication = rows with inline switches) — inconsistent mental model; (3) rows ran full page width on desktop, creating uncomfortable 1400px-wide measures for simple toggles. Redesign inspired by **Apple System Settings** (sidebar sections as rounded "group" cards with hairline dividers between rows), **Airbnb Account** (tight overline + card rhythm), and **Microsoft Settings** (uniform row anatomy). Changes to [src/pages/Preferences/index.tsx](src/pages/Preferences/index.tsx): **(a)** Introduced three local primitives: `<SectionTitle>` (uppercase overline, `mt: 3, mb: 0.75` — half the previous gap), `<SectionCard>` (rounded `12px` border, no `Card`/`CardContent` wrapper overhead), `<Row>` (uniform `px: 2, py: 1.5` flex row with `{label, description?, control}` — responsive: stacks on xs, row on sm+). **(b)** All sections now share this one Row primitive — Profile fields (name/email/timezone), Appearance (theme toggle), Communication (5 switches) use the same layout. **(c)** Profile photo row is the intentional exception (avatar + label + buttons needs a three-column layout) and sits above the first Divider inside the same card. **(d)** Page body wrapped in `maxWidth: 720` so rows don't run edge-to-edge on wide screens — matches Apple's fixed settings sheet width and keeps the eye-travel between label and control short. **(e)** Timezone row now uses the Row primitive: tz label + GMT offset become the row *description* (read-only state), "Change" becomes a text button with chevron on the right — shaving ~40px off the old bordered sub-row variant. Net result: ~40% less vertical space, single consistent scanning pattern, feels like a native settings sheet.

### Settings — Profile section
- **Move profile editing from inline dialog to a dedicated Profile section in Settings** — Editing identity (name / email / photo / timezone) now lives in the Settings page (`/preferences`), where it belongs alongside other "about me" settings rather than popping a modal on the read-only Profile page. Changes: **(1)** Added Profile fields to [src/store/slices/profileSlice.ts](src/store/slices/profileSlice.ts): new `guruPhoto: string | null` state (persisted to `localStorage` as `guru-photo`, stored as base64 data URL), new actions `setGuruEmail` and `setGuruPhoto`. **(2)** Added a **Profile** section as the first block of [src/pages/Preferences/index.tsx](src/pages/Preferences/index.tsx) with: a 72px circular `Avatar` (shows uploaded photo or initials fallback) + "Upload/Change" and "Remove" buttons backed by a hidden `<input type="file" accept="image/*">` (2 MB limit, image-type validation, toast on success/error); `TextField`s for full name and email; a bordered timezone row showing `PublicOutlinedIcon` + current tz label with a "Change" button that opens the existing global `TimezoneDialog` via `setOpenTimezone(true)`. **(3)** Added a `#profile` anchor on the section heading and a `useEffect` that scrolls the section into view when `location.hash === "#profile"`. **(4)** Edit Profile button on [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) now `navigate("/preferences#profile")` instead of opening the inline edit dialog (`useNavigate` added). Updated Settings subtitle to include "profile". The existing inline profile edit dialog in Profile/index.tsx is now dead code (no trigger remains); left mounted to keep this change contained — remove in a follow-up if confirmed unused.

### Profile — Identity Header
- **Swap tiny pencil IconButton for a properly-sized "Edit Profile" icon+text button** — The small `IconButton` pencil at the far-right of the row had a sub-44px tap target, no visible label (ambiguous affordance on mobile where there's no tooltip), and violated Gestalt proximity by sitting ~800px from the name it edits. Replaced with a MUI soft `Button` variant using `startIcon={EditOutlinedIcon}` + "Edit Profile" label, sized for touch (`minHeight: 40`, `px: 2`, `borderRadius: 10px`, `fontSize: 0.875rem`, `textTransform: none`). Positioned right-aligned within the same row (tooltip + `IconButton` wrapper removed). Icon 18px pairs with 14px text for a balanced pill. Label is always visible so there's no mobile affordance gap.
- **Shrink identity header to two lines; drop the avatar** — Three-line layout (name / email / timezone) still felt heavy, and the initials avatar added chrome without carrying information. Removed the `Avatar` entirely (and its `guruInitials` computation + MUI Avatar import) and collapsed email + timezone onto a single secondary line separated by a middle-dot divider: **Line 1** = name; **Line 2** = `{email} · 🌐 {timezone}`. Timezone segment still uses `PublicOutlinedIcon` and remains a click target that opens the timezone picker dialog. Email ellipsis-truncates when space is tight; timezone stays visible (flex-shrink 0). Pencil icon stays right-aligned.
- **Replace "Profile" title + 4-field identity card with a flat Apple/Linear-style identity row** — The old top section stacked three things (page title "Profile", the tagline "Your identity, performance trends, and financial overview.", and a 4-column card showing Name / Timezone / Primary mode / Programs) plus a soft "Edit profile" button — too much chrome for an identity block, wasting a full fold above the actual performance data. Replaced with a single compact row inspired by Apple System Settings' account header and Linear's workspace header: [Avatar 44–52px with initials]  **Name (h6)** + muted email + timezone as an inline `PublicOutlinedIcon` link — right-aligned pencil `IconButton` opens the existing edit dialog. Removed the "Profile" page title, the tagline, the identity card wrapper, and the "Primary mode" / "Programs" fields from the surface (those three fields still live in the edit dialog and in `profileSlice`, just not on the page header). Added `guruEmail: string` to [profileSlice.ts](src/store/slices/profileSlice.ts) with default `snehanjan.shome@greatlearning.in`. **Edit decision:** kept the inline edit dialog instead of routing to `/account` — only 3 fields, low friction, no context switch. Airbnb / Linear / Notion all keep quick identity edits inline; the `/account` and `/preferences` routes stay focused on preferences (notifications, theme), not identity.

### Profile — Performance Section
- **Neutralize Operational cards; keep only the sparkline colored** — With all categories' operational cards now visible at once, the stacked colored tints (info-bg, violet-bg, amber-bg, purple-bg) made the fold feel overwhelming and competed with the Ratings zone for attention. Ratings + ON-TIME CONFIRMS stay fully colored (headline zone), while Operational cards switch to neutral chrome: `background.paper` card bg, `text.secondary` label, grey-tinted CTA pill, neutral hover border/shadow. The sparkline still uses `card.accent` so the graph keeps its color identity per metric. Flag-driven via a `neutral: true` field set on each operational group card; [renderCard](src/pages/Profile/index.tsx) derives all chrome colors from that flag. Rating cards and `onTimeHeroCard` don't set the flag and keep their existing colored treatment.
- **Group Operational cards by category with per-group overline headings** — Replaced the single "Operational" overline + flat grid with one subsection per category. Introduced `operationalGroups` memo in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) returning `{ key, title, cards }[]`, one entry per `tabItems` entry (Evaluation / Moderation / Teaching / Mentoring). Render now iterates groups: each emits a `Typography variant="overline"` with the category name followed by a 2-column grid of that group's 2 cards. Removed the earlier TEACHING/MENTORING label prefix workaround — group heading now provides context, so cards keep clean labels ("AVG SESSIONS / MONTH"). Added stable per-card `id` (e.g. `Teaching:AVG SESSIONS / MONTH` vs `Mentoring:AVG SESSIONS / MONTH`) used as React key and for `reportModal` drawer lookup so duplicate labels across groups still route to the correct card's drawer. `operationalCards` kept as flat derived list (`operationalGroups.flatMap(g => g.cards)`) for `statCards` drawer lookup consumers.
- **Remove Operational tab grouping; show all cards at once** — The tabbed Operational zone (Evaluation / Moderation / Teaching / Mentoring chips) was hiding stats behind tabs. Users with multiple active roles could only see one category's operational metrics at a time. Refactored `operationalCards` in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) from tab-driven (`activeTab` picks one tab item) to visible across all `tabItems`. Removed `activeTab` state, the sync `useEffect`, and the `<Stack>` of tab chips from the render.

### Dev Panel — Version 1 Mode Toggle
- **Add "Version 1 Mode" toggle to Dev Panel** — New switch at top of the Dev Panel drawer (above Guru Role selector) to flag v1 ship scope. State lives in [src/store/slices/devPanelSlice.ts](src/store/slices/devPanelSlice.ts) as `isV1Mode: boolean`, persisted to `localStorage` under `guru-dev-v1-mode`. New actions `setV1Mode(boolean)` and `toggleV1Mode()` exported. UI in [src/components/dev/DevPanel.tsx](src/components/dev/DevPanel.tsx) uses a MUI `Switch` + `RocketLaunchOutlinedIcon`, labelled "Version 1 Mode" / "Show only v1 ship scope". Toggle is defined but not yet wired to any feature gate — downstream screens will read `s.devPanel.isV1Mode` to hide non-v1 functionality when the full app is built for v1 ship-readiness.

---

## 2026-04-14

### Profile — Performance Section
- **Merge ON-TIME CONFIRMS into the ratings row instead of a separate hero banner** — The full-width hero banner took too much vertical space, creating 3 stacked sections where 2 suffice. Moved ON-TIME CONFIRMS into the same grid as the per-category rating cards: section title becomes "Ratings & Health", grid is now up to 4 columns (1–3 ratings + 1 on-time). Single-role user sees 2 cards in one row (1 rating + on-time); multi-role sees up to 4. Removed the hero banner Card entirely, saving ~80px vertical space. Operational section stays as 2-card tabbed grid below.
- **Pull ON-TIME CONFIRMS out as a universal hero banner** — ON-TIME CONFIRMS was the only metric repeated identically in every category tab. Extracted it as a full-width hero stat card (Airbnb "Response rate" pattern) sitting between the Performance header and the Ratings zone. Always visible, never changes when tabs switch. Uses `selectedRole`'s data; clickable → opens the same right-side drawer. Styled as a bordered Card with success-accent tint, hero number at 1.75rem, inline delta + peer comparison, pill CTA. Operational tabs now show 2 cards each (was 3). Grid updated to `repeat(2, 1fr)`.
- **Redesign Performance section: Grouped Ratings + Tabbed Operational Metrics** — The Performance section was overloaded with up to 9 KPI cards in a carousel when multiple role categories were selected. Redesigned into two visual zones inspired by Airbnb/Apple/Stripe dashboard patterns: **Zone 1 (Ratings)** shows compact per-category rating cards side by side (1–3 depending on active roles) in a responsive grid — always visible, enabling instant cross-category comparison. **Zone 2 (Operational)** shows 3 operational cards (volume, quality/learners, on-time) in a tabbed layout — one tab per active category, switching the 3 cards below. Single-category users see no tab bar (just 1 rating + 3 operational = same density as before). Multi-category users see clear separation: ratings are the "headline" zone, operational metrics are "drill-in by category." Removed the carousel, floating arrows, scroll-snap logic, and carousel refs. The card rendering is extracted into a shared `renderCard` closure used by both zones. `statCards` is now `[...ratingCards, ...operationalCards]` for the drawer lookup; `ratingCards` and `operationalCards` are separate memos. Added `activeTab` state synced to `activeCategories` via `useEffect`.
- **Split AVG RATING into per-category rating cards** — The single combined "AVG RATING" card showed a weighted average of all active categories, which wasn't useful when users wanted to see each category's performance independently. Replaced with one rating card per active category: "TEACHING RATING", "MENTORING RATING", "EVALUATION RATING", "MODERATION RATING", or "EVALUATION & MODERATION RATING" depending on which roles are selected. Each card sources its data (overall, delta, trend, breakdown) from `demoRoleCategoryRatings[category]` and uses `categoryDisplayLabel()` for role-aware naming. Removed the per-category breakdown section from the drawer since each card now IS one category's detail. Added fallback map entries for all new label variants. In mixed mode, multiple rating cards appear (e.g. "TEACHING RATING" + "EVALUATION RATING").
- **Assign unique accent colors to eval/mod cards so no two cards share a colorway** — In mixed mode (7 cards from both sets), the eval cards were reusing amber/purple/green from the session cards, making them look like duplicates. Added `--gl-accent-info-bg`, `--gl-accent-violet-bg`, `--gl-accent-teal` + `--gl-accent-teal-bg` CSS variables (both light and dark mode) to the theme. Reassigned eval card colors: EVALUATIONS / MONTH → info (sky blue #0ea5e9), ON-TIME EVALUATIONS → violet (#7c3aed), LEARNERS IMPACTED → teal (#009688). All 7 cards in mixed mode now have distinct colors.
- **Replace hidden top-right chevron buttons with floating edge-anchored arrows on both carousels** — The tiny 30×30 icon buttons tucked into the top-right corner of the Performance KPI carousel and the "What learners say" testimonials carousel were disconnected from the scrollable content and easy to miss. Replaced with a proper carousel pattern: 36×36 circular arrow buttons positioned absolutely at the left/right edges of each carousel, vertically centered on the cards (`top: 50%, transform: translateY(-50%)`). White background with a soft `box-shadow: 0 2px 8px rgba(0,0,0,0.15)`, hover tints to grey.100. Desktop-only (`display: { xs: "none", sm: "flex" }`) — mobile users swipe naturally. Each carousel is now wrapped in a `position: relative` Box; scroll containers get `px: { sm: 1 }` so first/last cards don't hide behind the arrow buttons. Testimonials header "What learners say" is no longer in a row with buttons — it's a standalone heading above the carousel.
- **Multi-role union: show all unique KPI cards in a carousel when >4** — When the user selects roles from both categories (e.g. Teacher + Evaluator), all unique cards from both sets now render: AVG RATING (shared), AVG SESSIONS / MONTH, AVG SESSION QUALITY, ON-TIME CONFIRMS (from Teaching/Mentoring), EVALUATIONS / MONTH, ON-TIME EVALUATIONS, LEARNERS IMPACTED (from Eval/Mod) — 7 total. Each set sources data from its own category's first selected role. When the card count exceeds 4, the layout switches from a 4-column grid to a horizontal carousel (`overflowX: auto`, `scrollSnapType: x mandatory`, hidden scrollbar, `scrollSnapAlign: start` per card at 280px fixed width / 70vw mobile). Chevron nav arrows appear above the row for desktop scroll. Pure single-category selections still render the 4-card grid as before.
- **Bugfix: drive Performance card branching off `selectedRoles` (chips), not `selectedRole` (dropdown)** — Initial implementation gated the card-set switch on `selectedRole`, but the dev panel's "Active Guru Roles (Profile)" chips dispatch `toggleRole` which only mutates `selectedRoles` (the array), leaving `selectedRole` untouched. Result: toggling the Evaluator/Moderator chips appeared to do nothing — the cards stayed in set A. Now branches on a derived `isPureEvalMod` flag (`activeCategories.length === 1 && activeCategories[0] === "Evaluation & Moderation"`), and sources the underlying data from the first Eval/Mod role in `selectedRoles` when in pure-Eval/Mod mode (falling back to `selectedRole` otherwise). Mixed-category selections (e.g. Teacher + Evaluator) intentionally stay on set A so session metrics still render correctly. Memo dep array updated to include `selectedRoles`, `dataRole`, and `isEvalModCategory`.
- **Make Performance KPI cards role-aware (split by category)** — Previously every role saw the same four cards (AVG RATING, AVG SESSIONS / MONTH, AVG SESSION QUALITY, ON-TIME CONFIRMS). For Evaluators / Moderators these don't fit: "session quality" is framed around 4.0+/4.4+ session ratings (they evaluate assignments, not sessions); "sessions / month" reads "sessions" when the unit is really evaluations or moderations; "on-time confirms" describes confirming an assigned session, but evaluators don't confirm — they have a turnaround. Refactored `statCards` in [src/pages/Profile/index.tsx](src/pages/Profile/index.tsx) to branch on `ROLE_TO_CATEGORY[selectedRole]` and emit one of two card sets:
  - **Teaching & Mentoring** (Teacher, Industry Expert, Course/Career/CV Review/Project Mentor) → unchanged 4 cards.
  - **Evaluation & Moderation** (Evaluator, Moderator) → AVG RATING + EVALUATIONS / MONTH (or MODERATIONS / MONTH for Moderator) + ON-TIME EVALUATIONS / MODERATIONS + LEARNERS IMPACTED. The "session quality" card is dropped for this category; LEARNERS IMPACTED takes its place to mirror Tile 4 of the live production dashboard for this role group.
  - All Eval/Mod card copy (description, reportTitle, reportSummary, supportingStat label) is parameterised on the role's work noun ("evaluation" vs "moderation") so it reads naturally per role.
- **Add `learnersImpacted*` fields to RoleStatCardData** in [src/data/demo-sessions.ts](src/data/demo-sessions.ts) — `learnersImpactedPerMonth`, `learnersImpactedDelta`, `learnersImpactedBars`, `learnersImpactedBreakdown`, `peerLearnersImpactedPerMonth`. Populated for all 8 roles with realistic monthly counts (Teacher ~340, Course Mentor ~280, Career Mentor ~180, CV Review Mentor ~220, Project Mentor ~150, Industry Expert ~110, Evaluator ~320, Moderator ~210) and gentle 6-month upward trends. Today only the Eval/Mod cards consume this; the data is populated for all roles so the LEARNERS IMPACTED card can be reused for Teaching/Mentoring later without a data-shape change.
- **Update `sessionsBreakdown` units for Evaluator/Moderator** — values were "95 sessions" / "42 sessions" etc. which read awkwardly under an "Evaluations" or "Moderations" card label. Changed to "95 evaluations" (Evaluator) and "42 moderations" (Moderator) so the breakdown table inside the drawer matches the card framing.
- **Extend `diffStr` peer-comparison number formatter** for both the inline card pill and the drawer pill — added cases for the new labels: percent for `ON-TIME EVALUATIONS` / `ON-TIME MODERATIONS`, integer for `EVALUATIONS / MONTH` / `MODERATIONS / MONTH` / `LEARNERS IMPACTED` / `AVG SESSIONS / MONTH` (the integer case fixes a long-standing minor bug — sessions/month was previously formatted with 2 decimal places).
- **Add early-user / new-user fallback copy** for the 5 new label keys (`EVALUATIONS / MONTH`, `MODERATIONS / MONTH`, `ON-TIME EVALUATIONS`, `ON-TIME MODERATIONS`, `LEARNERS IMPACTED`) in `zeroMessages`, `earlyValues`, and `earlyDescriptions`, so Eval/Mod users in those states see role-appropriate placeholder copy rather than blank fallbacks.

---

## 2026-04-13

### Profile — Performance Section
- **Replace text arrows with MUI Material icons** — The `↗` / `↘` delta glyphs in the Performance KPI cards (delta indicator, inline category ratings) and the `→` in the "See detailed report" footer were plain Unicode characters. Swapped them out for MUI icons: `TrendingUpIcon` for positive delta, `TrendingDownIcon` for negative delta, and `ArrowForwardIcon` for the "See detailed report" chevron. Same treatment applied to the delta indicators inside the course detail drawer and the detailed report modal (active card delta + per-category ratings) for visual consistency across the Performance flow.
- **Expand ambiguous "Eval & Mod" label in AVG RATING breakdown** — The role-category row in the AVG RATING KPI card (and the detailed report modal) was showing the abbreviation "Eval & Mod" for the `Evaluation & Moderation` category. Replaced with a role-aware label: "Evaluation" when the user only has the Evaluator role, "Moderation" when only Moderator, and "Evaluation & Moderation" when they hold both. Removes the ambiguous short form and matches the user's actual role(s).
- **Move AVG SESSION QUALITY thresholds into the detailed report modal** — The "Quality thresholds" sub-panel (Rated 4.0+ / Rated 4.4+ with targets) now lives inside the "Session Quality Report" dialog instead of on the card. The card was duplicating the hero value ("Rated 4.0+" is the same number as the hero) and the stricter 4.4+ bar is drill-in detail, not at-a-glance info. In the modal, each row now reads as a full sentence — "Sessions rated 4.0 or above · You 97.5% · Target > 98%" — with a Met / Below target chip derived from the existing benchmark strings, so nothing that was on the card is lost on the click-through.
- **Lighten the dark green sentiment color across Performance** — The positive-trend / "you're ahead" indicators on the Performance KPI cards (delta line, peer comparison sentiment) and on related Performance widgets (Course Performance "Biggest gain" stat, per-course delta in the course detail drawer) were rendered in `success.dark`, which read as visually heavy. Switched all four occurrences to `success.main` for a brighter, more legible green that's consistent across the section.
- **Move the delta line inline next to the hero number on Performance KPI cards** — The trend chip (e.g. `↗ +0.08 vs last month`) used to sit on its own row underneath the hero value (e.g. `4.91`), so the user had to read two stacked rows to connect "the value" with "how much it changed". Wrapped the hero and the delta in a baseline-aligned `Stack` so the delta now renders to the right of the hero number on desktop, restoring proximity. Mobile keeps the delta hidden as before (whole card is tappable to drill in). The standalone desktop-only delta block was removed.
- **Straighten the delta row alignment** — The trend icon, `+0.8%` value, and `vs last month` label were drifting up and down because they were split across two `<Typography>` elements with mismatched alignment modes (icon+number used inline-flex centered, the label was on text baseline). Lifted the icon out of the value Typography, made all three direct children of the inline `Stack` with `alignItems: "center"`, and pinned each child to `lineHeight: 1` so they share one optical centerline.
- **Reduce KPI hero font from 2.25rem → 2rem (desktop)** — At 2.25rem the hero number (e.g. `97.5%`) was wide enough that the inline delta row (`↗ +0.8% vs last month`) wrapped to a second line on narrower card widths. Dropped the desktop `fontSize` to 2rem so the hero + delta consistently fit on one line. Mobile size (1.35rem) unchanged.
- **Remove per-role category breakdown from AVG RATING card** — The inline breakdown rows (`Mentoring 4.84 +0.08`, `Evaluation 4.98 +0.05`, etc.) on the AVG RATING KPI card were adding 2–3 extra rows of small-font text, making the card visually heavy. Removed from the card surface; the full per-category cards (with trend chart + breakdown rows) already exist in the "See detailed report" modal under "Rating by Role Category", so nothing is lost on drill-in.
- **Add breathing room between rows on Performance KPI cards** — After trimming content off the cards, the remaining rows (label → hero+delta → description → peer comparison → sparkline) had collapsed tight together. Bumped the vertical gaps so each row has a clear margin of air: hero Stack `mb` 1.5 → 2.5, description `mb` 1.5 → 2, and a guaranteed `pt: 3` above the sparkline chart (in addition to `mt: auto` which still pushes it to the bottom). Also cleaned up the peer benchmark's leftover conditional margin (previously toggled on `primaryBenchmark`, which no longer lives on the card).
- **Fix floating sparkline dots after padding bump** — Once `pt: 3` was applied to the sparkline wrapper, the absolutely-positioned hover-dot targets drifted above the line because their `top: (y/h * 100)%` was computing a percentage of the now-taller wrapper (SVG height + padding) while the SVG itself only occupied the lower portion. Wrapped the sparkline in an outer spacer Box that holds the `pt`/`mt: auto`, and kept the inner (SVG-sized) Box as the positioning context for the dots — so the dots snap back to sit exactly on the polyline.
- **Replace "See detailed report" footer with a pill icon-button in the label row** — Each KPI card had a divider + padded bottom strip with "See detailed report →" text, which added ~2 rows of visual weight per card and duplicated the "whole card is tappable" behavior the card already has. Swapped for a Swiggy-style affordance: a small pill-shaped IconButton sitting to the right of the card's accent label, containing an `ArrowForwardIcon`. The button has a "See detailed report" tooltip for discoverability, stops click propagation so the card's own onClick doesn't double-fire, and removes the bottom strip entirely — shaving ~40–50px off each card's height.
- **Polish the pill icon-button visuals** — Initial version was a 26×26 circle with a visible border; not a true pill and not themed to the card. Reworked: 38×24 (wider than tall — actual pill/stadium shape), no border, background is the card's own accent color at ~16% alpha via `color-mix(in srgb, <accent> 16%, transparent)`, arrow rendered in the full accent color. Hover bumps bg to ~26% alpha and slides the arrow 2px right via a className-targeted transform; active state adds a subtle `scale(0.96)` for tactile feedback. Ripple disabled for a cleaner feel. Each of the 4 cards now gets a pill in its own colorway (primary/warning/purple/success) that reads as part of the card instead of a generic neutral chip.
- **Increase gap between card content and sparkline** — The chart still felt visually crowded against the peer-comparison line above it. Bumped the sparkline wrapper's `pt` from `{ xs: 1.5, sm: 3 }` to `{ xs: 2.5, sm: 5 }` (≈40px breathing room on desktop), so the chart sits clearly separated from the textual content.
- **Make Performance KPI cards feel interactive — pointer cursor + hover lift** — The cards were clickable (whole-surface onClick opens the detailed report modal) but gave no visual affordance on desktop: cursor stayed as `default` and there was no hover state, so users couldn't tell the card was a button. Switched `cursor` to `pointer` on all breakpoints (gated by `!isNewOrEarly`), and added a hover state that lifts the card 2px (`translateY(-2px)`), casts a soft drop shadow tinted with the card's own accent (`box-shadow: 0 6px 18px -6px color-mix(accent 35%, transparent)`), and strengthens the border to a 55%-alpha accent. Active state reduces the lift to 1px for a tactile press. Transitions `border-color`, `transform`, and `box-shadow` together at 0.2s ease. Each card's hover shadow picks up its own accent so the four cards feel distinct but consistent.
- **Reframe AVG CONFIRM TIME → ON-TIME CONFIRMS so all four KPI cards share "higher is better" semantics** — Previously three cards (rating, sessions, quality) were "higher = good, ↗ green = improving" but AVG CONFIRM TIME flipped the convention (lower hours = good, ↘ down-trending chart actually meant improvement). This created cognitive friction every time the eye landed on the fourth card. Replaced the metric on the card surface with **ON-TIME CONFIRMS** — the % of sessions confirmed within the 24-hour SLA — so up-arrow/up-trend always means "good" across the whole row. Added new fields to `RoleStatCardData` (`onTimeConfirmRate`, `onTimeConfirmDelta`, `onTimeConfirmBars`, `onTimeConfirmBreakdown`, `peerOnTimeConfirmRate`) populated for all 8 roles, with realistic 6-month upward trends. The raw average confirm time isn't lost — added a new optional `supportingStat` field on the card config that the detailed report modal renders as a small "Average time to confirm: 5.4h" line right under the hero+delta row, so anyone drilling in still sees the underlying time number.
- **Neutral background for the peer comparison pill inside the Performance drawer** — The peer pill inside the right-side detailed report drawer was using the card's accent color at 10% alpha (`color-mix(in srgb, <accent> 10%, transparent)`), which on the purple AVG SESSION QUALITY drawer painted the pill with a lavender tint that competed with the green "You're 2.5% ahead" sentiment text in the foreground. Switched the pill background to the theme's neutral `action.hover` token so the colored foreground (success.main / warning.dark) reads clearly against it, regardless of which card was clicked.
- **Move the Performance detailed report from a centered dialog to a right-side drawer** — The "See detailed report" view was rendering as an MUI `Dialog` (centered, fixed width ~600px, short vertical height with internal scroll). Switched to a right-anchored `Drawer` that fills the full viewport height and is 480px wide on desktop / 100vw on mobile. Replaced `DialogContent` with a flex-1 `Box` (`overflowY: auto`) so long content scrolls inside the drawer rather than pushing the footer off-screen, and replaced `DialogActions` with a bordered footer row. Keeps the same close/dismiss behavior (backdrop click, Close button) and all the existing content blocks (hero + delta, peer comparison, supporting stat, trend chart, quality thresholds / per-category sections, breakdown table) — it's a container swap, not a content change.
- **Remove "SLA" jargon from ON-TIME CONFIRMS copy** — User-facing strings referenced "the 24-hour SLA" — a B2B ops term many learners/gurus wouldn't know. Rewrote to plain language: the card description is now "Sessions you confirmed within 24 hours of being assigned." and the detailed report summary is "Share of sessions you confirmed within 24 hours of being assigned. Higher is better." Internal JSDoc comments in `RoleStatCardData` also cleaned up. Same data, just clearer copy.
- **Bump sparkline gap further** — Increased the breathing room above the chart on each Performance KPI card again: sparkline wrapper `pt` now `{ xs: 3.5, sm: 7 }` (≈56px desktop, was 40px). Mobile bumped from 20px → 28px. The chart now sits with a clear visual break from the peer comparison row above it.
- **Sync card → modal data parity for all four Performance cards** — Audit found three mismatches between what each KPI card showed on its surface and what its "See detailed report" dialog rendered: (a) the **peer comparison** ("Peer avg X · You're Y ahead/to go") was visible on every card but completely missing from every modal; (b) the **card description** sentence (e.g. "Sessions you confirmed within the 24-hour SLA") was on every card but not in the modal header; (c) the early-user **fallback maps** (`zeroMessages`, `earlyValues`, `earlyDescriptions`) still keyed off the old `"AVG CONFIRM TIME"` label after the rename, so early users saw blank fallback content for the renamed card. Fixed all three: the modal now renders a soft-tinted peer comparison pill under the hero+delta row using the same sentiment logic and units as the card; the description is rendered as a small caption directly under the report summary in the modal header; and the fallback map keys updated to `"ON-TIME CONFIRMS"` with appropriate copy ("On-time confirmation rate appears as you confirm sessions").

---

## 2026-03-19

### Home Page
- **Rename "Sessions" to "Events" throughout Dashboard** — Changed all user-facing labels: "Next Session" → "Next Event", "Sessions" → "Events", etc.

### Components Page (Dev Mode) — UX Audit
- **Consistent card structure** — All cards now follow the same 3-row layout: title + status, date/batch info line, actions + View details. No more floating secondary info.
- **View Details dialogs for all types** — Every activity type (Residency, Online Session, Career, Evaluation, Moderation, Capstone, CV Review) now has a working View Details dialog showing secondary info (group, topic, contact, course link, milestone dates, etc.).
- **Status chips replace floating text** — "Gathering feedback", "No feedback collected", "Payment pending", "Payment processed" are now consistent Chips with proper styling instead of loose Typography.
- **Secondary info moved to dialogs** — Group name, topic, contact email, city/map, LMS links, Create Poll, Student Progress, View LinkedIn/CV, milestone dates all moved into View Details.
- **Disabled state for pending payments** — "View in payments" button is now disabled when payment is pending.
- **View details button always right-aligned** — Consistent positioning across all card types.

---

## 2026-03-18

### Home Page
- **Add Planned Events section** — New "Planned Events (subject to change)" section inside the Upcoming tab on the Home page. Shows date range, session type, batch, contact email, and "To be confirmed" status. Disappears when switching to Completed or Declined tabs. Added `PlannedEvent` type and demo data.

### Components Page (Dev Mode)
- **Add Components page** — New `/components` route accessible from the Dev Panel. Entry point added under "Dev Tools" section.
- **Persona-based card variants** — Components page now reacts to the selected Guru Role from the Dev Panel. Each role shows its relevant activity types with Confirmed, Tentative, Scheduled, and Completed card states:
  - **Teacher**: Residency + Online Session
  - **Course Mentor**: Online Session + Residency
  - **Career Mentor**: Career/Mock Interview Sessions + CV Review
  - **CV Review Mentor**: CV Review
  - **Evaluator**: Evaluation (Assignment) with star-icons-only rating
  - **Moderator**: Moderation (Discussion Question) with star-icons-only rating
  - **Project Mentor**: Capstone Project with milestone dates
  - **Industry Expert**: Online Session

---

## 2026-03-17

### Course Page
- **Fix course card thumbnails** — Added missing `color` and `pattern` fields to all 33 entries in `demoCourseCatalog` (`src/data/demo-sessions.ts`). Cards were rendering as plain blue without patterns; now each course has a unique color and SVG pattern overlay.

### Session Cards
- **Reduce title font size** — Lowered `SessionCard` title font to `1rem/1.125rem` (h5) and `0.95rem` (h6) so it fits the overall page scale (`src/components/shared/SessionCard.tsx`).

### Dashboard Home — Tasks Sidebar
- **Move chips inline with title** — "Configured" / "Action needed" chips now sit to the right of each task card title instead of above it (`src/pages/Dashboard/index.tsx` — `TaskCard`).
- **Availability accordion** — When the user has configured availability, the summary renders inside a collapsed `Accordion`; expands on click. Unconfigured state still shows the regular task card with "Needs update" chip.
- **Rename confirm sessions task** — Title changed from "Confirm sessions by Wednesday" to "Confirm upcoming sessions". Wednesday deadline moved to the description line.

### Dashboard Home — UX Simplification
- **Next Session pulled out above tabs** — Standalone "Next Session" section with 1 hero card (blue left border) now sits above the Sessions card. Shows "Join session" + "View details" only.
- **Flat Upcoming tab** — Removed the nested "Up next" `Paper` wrapper and overline labels. Upcoming tab now has two flat sections: **Scheduled** (with count chip) and **Confirmed** (with count chip), separated by a divider.
- **Hero session de-duplicated** — The next session is filtered out of the Scheduled list so it doesn't appear twice.
- **Reduced hero actions** — Removed "Session Materials" and "Create poll" buttons from the hero card (moved to detail view). Only 2 actions remain.
- **Cleaned unused imports** — Removed `MenuBookOutlinedIcon`, `setOpenPollBuilder`, `setPollSessionId`, `setPollEditingId`, `setPollQuestion`, `setPollOptions`, `removePoll` from Dashboard imports.

### Navigation
- **Sidebar collapsed by default** — Left nav now starts in collapsed (icon-only, 80px) state (`src/store/slices/uiSlice.ts` — `isNavCollapsed: true`). Users can expand via the hamburger icon.
- **Replaced Alerts with Payments in sidebar** — Removed the "Alerts" nav item (with notification badge) and added "Payments" with wallet icon (`src/components/layout/Sidebar.tsx`). Cleaned up `Badge`, `NotificationsOutlinedIcon`, and `unreadCount` from sidebar.

### Payments Page (new)
- **Created `src/pages/Payments/index.tsx`** — New dedicated Payments page with Earnings overview (total, avg/month, best month, MoM trend, bar chart) and Payment details table (session, type, duration, amount, status, transaction ID, invoice).
- **Route added** — `/payments` route registered in `src/App.tsx`.

### Profile Page
- **Removed Earnings & Payments sections** — Earnings bar chart, KPI row, and payments table moved to the new Payments page. Profile now focuses on identity + performance only. Cleaned up unused imports (`BarChart`, `Bar`, `BarChartOutlinedIcon`, `AccountBalanceWalletOutlinedIcon`, `CreditCardOutlinedIcon`, `DescriptionOutlinedIcon`, `demoMonthlyEarnings`).

### Settings Page (renamed from Preferences)
- **Renamed "Preferences" → "Settings"** — Nav label changed from "Prefs" to "Settings" in sidebar (`src/components/layout/Sidebar.tsx`). Page title updated to "Settings" (`src/pages/Preferences/index.tsx`).
- **Dark mode toggle moved from sidebar to Settings** — Removed the dark/light mode toggle from the bottom of the left nav. Added it under a new **Appearance** section in Settings with a switch and icon.
- **Communication section** — Existing notification/email preferences now grouped under a **Communication** heading, visually separated from Appearance. Both sections use Card wrappers for cleaner structure.
- **Sidebar header icon swap on hover** — The hamburger menu button now shows the Great Learning "G" logo by default (collapsed only). On sidebar hover, it crossfades to the hamburger menu icon (ChatGPT-style interaction). Expanded mode always shows the hamburger since the GL logo is already visible.
- **Nav items: primary active state + ripple** — Active nav items now use a primary-tinted background (`--md-primary-container`) with `--md-on-primary-container` text/icon color. Both collapsed pill and expanded row use MUI `ButtonBase` for proper press ripple and hover feedback. Added `--md-primary-container` / `--md-on-primary-container` CSS tokens for both light and dark themes (`src/index.css`).

### Dashboard Home — Section Titles
- **Smaller font + icons for section titles** — "Next Session", "Sessions", and "Tasks" titles reduced from `h6` to `subtitle1` at `0.9rem`. Each now has an icon: `PlayCircleOutlined` (Next Session), `ViewListOutlined` (Sessions), `AssignmentOutlined` (Tasks) in `text.secondary` color.

### Dashboard Home — Next Session Card
- **Session Materials button restored** — Re-added "Session Materials" outlined button (with `MenuBookOutlinedIcon`) to the Next Session hero card actions, between "Join session" and "View details" (`src/pages/Dashboard/index.tsx`).

### Session Details Modal
- **Create New Poll button for confirmed sessions** — Added "Create New Poll" outlined button (with `PollOutlinedIcon`) to `SessionDetailsModal` dialog actions. Only visible for confirmed, non-completed sessions. Opens the Poll Builder dialog with a fresh form pre-linked to the session (`src/components/dialogs/SessionDetailsModal.tsx`).
